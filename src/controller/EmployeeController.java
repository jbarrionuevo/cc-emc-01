package controller;

import model.CallCenter;

public class EmployeeController {
	
	CallCenter callCenter;
	
/*	public Employee getAvailableEmployeeByType(String t){
		int i = 0;
		Employee e = null;
		while (i<callCenter.getEmployees().size()) {
			e = employees.get(i); 
			if(e.getType().equals(t) && e.getStatus().equals("available")){
				break;
			}
			i++;
		}
		return e;
	}*/
}
